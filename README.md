1. to install all lib requred:  **npm install**
2. run the following command in terminal to get the exe version on your machine :      **npm run electron:build**
3. to run the backend on local device:  **npx vercel dev**
4. to run the backend on local device:  **npm run dev**
