
1. run the following command in terminal to get the exe version:      **npm run electron:build**
    the installer will be in the folder named Desktop build

2. to run the backend on local device:  **npx vercel dev**
3. to run the backend on local device:  **npm run dev**
